<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/modelCard"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:layout_margin="2dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="1dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="@color/model_card_background">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <!-- Model Icon at top center -->
        <ImageView
            android:id="@+id/circleIcon"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_centerHorizontal="true"
            android:layout_alignParentTop="true"
            android:layout_marginTop="1dp"
            android:src="@drawable/ic_default_model"
            android:scaleType="centerInside" />

        <!-- Model Name below icon -->
        <TextView
            android:id="@+id/tvModelName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/circleIcon"
            android:layout_marginTop="4dp"
            android:text="GPT-4o mini"
            android:textSize="11sp"
            android:textColor="?attr/colorOnSurface"
            android:textStyle="bold"
            android:gravity="center"
            android:maxLines="1"
            android:ellipsize="end"
            android:fontFamily="@font/ultrathink" />

        <!-- Provider chip with icon and name -->
        <LinearLayout
            android:id="@+id/providerChip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvModelName"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="2dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:background="@drawable/provider_chip_background">

            <!-- Provider icon -->
            <ImageView
                android:id="@+id/providerIcon"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_marginEnd="4dp"
                android:src="@drawable/ic_gpt"
                android:scaleType="centerInside" />

            <!-- Provider name -->
            <TextView
                android:id="@+id/tvProviderName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="OpenAI"
                android:textSize="8sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textStyle="normal"
                android:maxLines="1"
                android:ellipsize="end"
                android:fontFamily="@font/ultrathink" />

        </LinearLayout>

        <!-- Pro badge in top right corner -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipPro"
            android:layout_width="wrap_content"
            android:layout_height="14dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_margin="1dp"
            android:text="PRO"
            android:textSize="7sp"
            android:textColor="@android:color/white"
            android:visibility="gone"
            app:chipBackgroundColor="@color/accent_element"
            app:chipCornerRadius="7dp"
            app:chipMinHeight="14dp"
            app:textStartPadding="3dp"
            app:textEndPadding="3dp" />


    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>